<% if current_announcement %>
	<div id="starburst-announcement">
	 	<p><%= current_announcement.body.html_safe %></p>
	 	<%= link_to "&times;".html_safe, starburst.mark_as_read_path(current_announcement.id), :method => :post, :remote => true, :id => "starburst-close" %>
	</div>
<% end %>
